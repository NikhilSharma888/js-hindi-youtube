"use strict"; // treat all JS code as newer version

// alert( 3 + 5 ) // we are using nodejs, not browser, so alert will not work

console.log(3
    + 5) // code readability should be high priority, so use proper indentation and line breaks

    console.log("Nikhil")
     
    let name = "Nikhil"
    let age = 20
    let isLoggedIn = false
    let state = null


    // Given below are the data types in JS //

    // number => 2 to the power 53
    // bigint => larger than 2 to the power 53
    // string => any text within single or double quotes
    // boolean => true or false
    // null => empty value,  
    // undefined => value is not assigned
    // symbol => unique value that cannot be changed

    // object => key value pair, can hold multiple values

    console.log(typeof "Nikhil") 
    console.log(typeof 20)
    console.log(typeof 20n)
    console.log(typeof undefined) // undefined type is undefined 
    console.log(typeof null) // null type is object